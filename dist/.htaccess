RewriteEngine On

# SPA fallback - all non-file/directory to index.html. Article Open Graph
# metadata is served by the Node/Express frontend server, not PHP.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ /index.html [L]
