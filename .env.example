# Production settings for the combined Express API and React application
NODE_ENV=production
PORT=4000
MONGO_URI=mongodb+srv://hexabrandingofficial_db_user:PASSWORD@cluster0.8fxgxzx.mongodb.net/?appName=Cluster0
JWT_SECRET=replace-with-a-long-random-secret
FRONTEND_URL=https://demo.malayalamitharam.in
ALLOWED_ORIGIN=https://malayalamithram.in
# Used only when the MongoDB database has no admin user yet.
ADMIN_INITIAL_PASSWORD=replace-with-a-strong-admin-password
# Set this before running npm run build so the browser uses the same site's API.
VITE_API_URL=/api
